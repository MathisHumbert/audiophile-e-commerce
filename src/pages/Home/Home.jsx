import styled from 'styled-components';
import Hero from './components/Hero';

const Home = () => {
  return (
    <Wrapper>
      <Hero />
    </Wrapper>
  );
};

const Wrapper = styled.main``;
export default Home;
